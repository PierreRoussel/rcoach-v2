# Copier ce fichier en `android/keystore.properties` (non versionné).
# Le keystore lui-même reste hors dépôt — voir docs/play-store-compliance.md.

storeFile=app/upload-keystore.jks
storePassword=REMPLACER
keyAlias=upload
keyPassword=REMPLACER
