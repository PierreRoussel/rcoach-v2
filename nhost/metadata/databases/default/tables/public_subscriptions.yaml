table:
  name: subscriptions
  schema: public
object_relationships:
  - name: user
    using:
      foreign_key_constraint_on: user_id
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - user_id
        - tier
        - status
        - billing_period
        - current_period_end
        - provider
        - provider_ref
        - trial_consumed_at
        - created_at
        - updated_at
      filter:
        user_id:
          _eq: X-Hasura-User-Id
